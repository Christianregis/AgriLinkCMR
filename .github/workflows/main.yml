name: SIMPLE WORKFLOW CI/CD

on: 
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  test-code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test Hello 
        run: echo "Build..."
  
